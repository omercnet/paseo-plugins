setupVersion: 2
defaultThinkingLevel: off
memory:
  backend: off
modelRoles:
  default: canary-mock/deterministic
  plan: canary-mock/deterministic
  slow: canary-mock/deterministic
  advisor: canary-mock/deterministic
  task: canary-mock/deterministic
  vision: canary-mock/deterministic
  designer: canary-mock/deterministic
  smol: canary-mock/deterministic
  tiny: canary-mock/deterministic
  commit: canary-mock/deterministic
enabledModels:
  - canary-mock/deterministic
  - canary-ollama/qwen2.5:0.5b
modelProviderOrder:
  - canary-mock
  - canary-ollama
retry:
  enabled: false
  modelFallback: false
  usageAwareFallback: false
dev:
  autoqaConsent: denied
